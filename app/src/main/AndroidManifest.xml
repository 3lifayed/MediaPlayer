<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/cover_art"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/cover_art"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Developer Info -->
        <meta-data
            android:name="com.example.developer"
            android:value="Ali Fayed" />
        <meta-data
            android:name="com.example.copyright"
            android:value="© 2025 Ali Fayed. All rights reserved." />

        <!-- Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Player Activity -->
        <activity android:name=".PlayerActivity" />

    </application>
</manifest>
